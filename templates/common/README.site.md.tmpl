# {{SITE}} — 部署摘要

由 site-deploy SOP 管理;流程一律以 `/site-deploy` 觸發,詳見專案 `CLAUDE.md`。

| 項目 | 值 |
|---|---|
| 網域 | {{DOMAIN}} |
| SSH host | (見 `deploy/deploy.conf` 的 `SSH_HOST`) |
| stack | (見 `deploy/deploy.conf` 的 `STACK`) |
| 遠端目錄 | `/srv/{{SITE}}` |

## 特殊事項

<!-- 本站專屬注意事項:排程、queue、第三方服務、資料匯入…沒有就留空 -->

- (無)
