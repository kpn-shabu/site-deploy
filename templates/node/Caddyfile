# {{DOMAIN}} 由 Caddy 自動申請與續約 Let's Encrypt 憑證
{{DOMAIN}} {
	# 反代到 compose service app(node 站聽 3000)
	reverse_proxy app:3000
}

# www 一律 301 轉址到 apex
www.{{DOMAIN}} {
	redir https://{{DOMAIN}}{uri} permanent
}
